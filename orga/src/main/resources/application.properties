spring.application.name=orga

# PostgreSQL datasource (adjust to your local DB)
# spring.datasource.url=jdbc:postgresql://localhost:5432/final-orga
# spring.datasource.username=postgres
# spring.datasource.password=root
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.hibernate.ddl-auto=update

# server.port=8080

# Server
# ===============================
server.port=${PORT:8080}
# ===============================
# PostgreSQL Database
# ===============================
# For production (Render): Use environment variables
# For local dev: Use the hardcoded values below or set env vars
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://ep-spring-flower-ah5e4r87-pooler.c-3.us-east-1.aws.neon.tech:5432/orga-final?sslmode=require}
spring.datasource.username=${DB_USERNAME:neondb_owner}
spring.datasource.password=${DB_PASSWORD:npg_zGw9VSATQR0L}
spring.datasource.driver-class-name=org.postgresql.Driver

# ===============================
# JPA / Hibernate
# ===============================
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=${SHOW_SQL:true}
spring.jpa.properties.hibernate.format_sql=${FORMAT_SQL:true}

# JWT config (256-bit secret for HMAC-SHA256)
# For production: Set JWT_SECRET as environment variable
app.jwt.secret=${JWT_SECRET:lZB52evfOi3gpHJ2OmLnsD53W6hH9abX1gZvqRkfcJo=}
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

# Python chatbot base URL
# For production (Render): Set CHATBOT_BASE_URL environment variable
chatbot.base-url=${CHATBOT_BASE_URL:http://127.0.0.1:8000}

# ===============================

